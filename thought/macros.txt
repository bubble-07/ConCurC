Macro Pass

Step 1:
Traverse outermost namespace in file, loading type declarations into memory

    Have a generic method to walk a namespace for type declarations
    Hold these into a static "definition table" containing declarations
    A "definition table" 


General calling convention:

    Equivalent to moving something around
